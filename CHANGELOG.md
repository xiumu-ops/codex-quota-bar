# 修改日志

本文件记录 Codex-Quota-Bar 各版本的重要变更。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [2.7.2] - 2026-09-07

### 变更

- 统一全应用圆角体系：主窗口外框与迷你模式外框基准圆角统一为 `8 px`，展开态统计子卡片圆角对齐为 `6 px`，与系统菜单和外观弹窗形成严谨的 Windows 11 原生设计层级。
- 优化 Direct2D 分层外描边算法，将描边半径同步内缩 0.5 px，保证描边与背景填充完全同心，消除对角线 0.707 px 的亚像素错位与抗锯齿晕染。
- 将项目版本、运行时版本、安装器、卸载器、资源清单和构建产物名称统一升级到 2.7.2。

### 修复

- 修复透明分层窗口（`WS_EX_LAYERED`）因设置 DWM 硬件圆角偏好而产生的暗色边缘虚边与阴影冲突问题，窗口圆角完全交由 Direct2D 逐像素 Alpha 掌控。


### 新增

- 新增可持久化的迷你模式：双额度使用 `240 × 48`，单额度或初始无额度使用 `120 × 48`，圆角半径保持 16 px。
- 迷你栏仅显示额度百分比和缩短的胶囊进度条；双额度按窗口、每周从左至右排列并保留主题分隔线。

### 变更

- 迷你模式直接复用标准折叠栏的百分比取整、进度比例、最小可见填充、颜色阈值、字体、透明度及其他运行配置。
- 将项目版本、运行时版本、安装器、卸载器、资源清单和构建产物名称统一升级到 2.7.1。

## [2.6.1] - 2026-09-06

### 新增

- 右键菜单新增“置顶模式：开 / 关”，状态保存到 `config-users.json`，重启后继续生效。

### 变更

- 将项目版本、运行时版本、安装器、卸载器、资源清单和构建产物名称统一升级到 2.6.1。

### 修复

- 支持免费方案只返回单一配额窗口的情况：该额度及其重置时间固定显示在“窗口使用限额”，未返回的“每周使用限额”保持不可用；同时优先读取 `rateLimitsByLimitId.codex`，避免有效额度和刷新时间被旧兼容字段覆盖。
- 额度窗口或对应重置时间未返回时，详情文字统一显示“官方当前未返回”，不再引入额外的“重置时间未知”状态。

## [2.5.8] - 2026-08-29

### 新增

- “编辑个性外观”窗口提供 19 项颜色输入、即时色块预览，以及 0%–90% 的背景透明度输入。
- 个性外观编辑器新增“应用 / 确定 / 取消”操作；“应用”保存但不关闭窗口，“确定”保存并关闭。
- 外观配置新增 `BackgroundTransparency` 字段及严格的类型、整数和范围校验。

### 变更

- 将右键菜单“外观”改为“外观配置”，“使用自定义外观”改为“使用个性外观”；内部 `Mode: "Custom"` 保持兼容。
- 外观配置菜单精简为“使用默认外观 / 使用个性外观 / 编辑个性外观”，移除高级配置文件入口、重新加载入口和独立透明背景入口。
- 点击“使用默认外观”或“使用个性外观”时重新读取并应用对应配置；默认外观固定使用默认配置中的 0% 透明度。
- 主窗口改用逐像素 Alpha 渲染，透明度仅作用于主卡片和展开态子卡片背景，前景元素与右键菜单保持不透明。
- 配置拆分为程序目录下的 `config-default.json` 与数据目录下的 `config-users.json`；默认模式不再直接依赖 C++ 配色常量，个性配置按字段覆盖默认基线。
- 取消旧版 `config.json` 的读取与自动迁移，仅支持 `config-default.json` 和 `config-users.json`；安装、升级回滚和卸载流程同步管理默认配置文件。
- 将项目版本、运行时版本、安装器、卸载器、资源清单和构建产物名称统一升级到 2.5.8。

## [2.5.3] - 2026-08-27

### 新增

- 新增“默认 / 自定义”外观模式，以及打开配置文件和运行时重新加载外观的右键菜单入口。
- 自定义外观支持系统字体族与 19 个语义颜色字段，主界面和自绘菜单共享同一套配置。
- 新增外观配置单元测试和完整的手工配置示例。

### 变更

- 将统一配置格式升级为版本 2，并在已有设置、窗口位置和外观配置之间保持兼容。
- 将项目版本、运行时版本、安装器、卸载器、资源清单和构建产物名称统一升级到 2.5.3。

### 修复

- 严格校验外观模式、字体名称和 `#RRGGBB` 色值；非法字段逐项回退并报告具体路径。
- JSON 损坏或外观字段无效时不再以默认内容覆盖用户文件。
- 修改缩放、刷新间隔、伴随模式或窗口位置时保留磁盘上的手工外观配置。

## [2.5.2] - 2026-08-26

### 变更

- Token 缩写统一保留两位小数，并在数字与单位之间增加空格。
- 补充相应的 App Server 集成测试覆盖。
- 准备 SignPath Foundation 审核所需的未签名版本；受信签名与人工批准流程仍以后续接入为准。

## [2.5.1] - 2026-08-26

### 新增

- 新增隐私政策与代码签名政策。
- GitHub Actions 新增语义化标签校验、稳定版 GitHub Release 发布和版本化安装包上传。

### 变更

- 远程构建环境迁移至 Windows Server 2025，并更新所用 Actions 运行时。
- 强化安装器生命周期隔离和 CI 环境清理。

### 修复

- 修复命名管道重建竞态。
- 补充 COM GUID 声明，保证 MSVC 链接稳定。

## [2.5.0] - 2026-08-26

### 变更

- 统一为当前用户安装和数据布局：程序位于 `app`，配置与日志位于 `data`。
- 安装、卸载、Hook、伴随启动项和快捷方式统一限定在当前用户范围。
- 强化运行时、命名管道、安装回滚及安装/卸载集成测试。

## [2.4.2] - 2026-08-24

### 新增

- 首个公开版本：提供 Direct2D 配额悬浮栏、App Server 同步、统计详情、Hook、CLI 和安装器。

### 变更

- README 对齐实际界面尺寸、右键菜单和交互逻辑。
